import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  text_input: {
      width: 200,
      height: 42,
      padding: 10,
      margin: 5,
      backgroundColor: '#eee',
      fontSize: 14,
  },
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});